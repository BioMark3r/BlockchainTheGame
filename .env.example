# WebSocket server URL for the client to connect to
# In production, set this to your deployed server URL (e.g. wss://your-app.railway.app)
VITE_WS_URL=ws://localhost:3001

# Port the WebSocket server listens on
PORT=3001
